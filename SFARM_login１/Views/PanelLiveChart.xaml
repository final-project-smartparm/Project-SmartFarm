<UserControl 
    x:Class="SFARM.Views.PanelLiveChart"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:SFARM.Views"
        xmlns:live="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Height="900" Width="800">
    <Grid Margin="0,0,0,0">
        <Grid x:Name="contentGrid" Margin="0,20,0,10" Height="500" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                
            </Grid.RowDefinitions>
            <!--In Code-->

            <!--실제 측정 값-->
            <live:CartesianChart 
        x:Name="chart" 
        BorderBrush="#467362" BorderThickness="2"
        LegendLocation="Top"
        Margin="0,0,0,34" HorizontalAlignment="Center" Width="720" Loaded="chart_Loaded"/>
            <!--실제(설정) db 정보 값-->
            <live:CartesianChart 
        x:Name="chart1" 
        BorderBrush="#467362" BorderThickness="2"
        LegendLocation="Top" Grid.Row="2"
        Margin="0,0,0,34" HorizontalAlignment="Center" Width="720"/>


        </Grid>

    </Grid>
</UserControl>
